import { createContext } from "react";

/**
 * Crée un contexte réutilisable à travers l'application
 * 
 * Permet d'accéder aux données fetch à travers l'application
 */
export const DataContext = createContext();